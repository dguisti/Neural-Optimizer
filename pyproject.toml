[build-system]
build-backend = "poetry.masonry.api"
requires = ["poetry>=0.12"]

[tool]
[tool.poetry]
authors = ["dguis <support@d-multi.com>"]
description = ""
name = "neural-optimizer"
version = "0.1.0"
[tool.poetry.dependencies]
python = "^3.7"
pip = "^20.0"
[tool.poetry.dev-dependencies]
